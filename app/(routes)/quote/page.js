import { Feedback, QuoteContainer } from '../../../sections';

const Page = () => (
  <div className="theme-dark-blue bg-skin-fill">
    <QuoteContainer />
    <Feedback />
  </div>
);

export default Page;
